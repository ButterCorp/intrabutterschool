import { SemanticSidebarDirective } from './semantic-sidebar.directive';

describe('SemanticSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new SemanticSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
