import { SemanticStickyDirective } from './semantic-sticky.directive';

describe('SemanticStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new SemanticStickyDirective();
    expect(directive).toBeTruthy();
  });
});
